const ROLE = [
  'admin',
  'seller',
  'buyer',
];

const PAGINATION = {
  INDEX: 1,
  SIZE: 20,
}

const STATUS_ORDER = ['watting', 'cancel', 'delivering', 'done']

module.exports = {
  ROLE,
  PAGINATION,
  STATUS_ORDER,
}